// header inclusion
#include <stdio.h>
#include <stdio.h>
#include <vector>
#include <map>
#include <string>
#include <opencv/cv.h>

std::vector <cv::Rect > getFaceData(std::string file);
std::vector <cv::Rect > getDartData(std::string file);